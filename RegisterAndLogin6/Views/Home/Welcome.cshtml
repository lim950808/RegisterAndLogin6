@model RegisterAndLogin6.Models.Account
@{
    ViewBag.Title = "Welcome";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Welcome</title>
</head>
<body>

    <h2>환영합니다. @(Session["UserId"].ToString())님</h2>
    <hr />
    @if (Session["UserId"] != null)
    {
        <text>
            @Session["UserId"].ToString()님, 회원가입이 완료되었습니다!!
        </text>
    }
    <br />
    <br />
    <a href="@Url.Action("MyInfo", "Account")">내 정보</a>
    ㅣ
    <a href="@Url.Action("Logout", "Account")">로그아웃</a>
</body>
</html>